---
import BaseLayout from "@layouts/BaseLayout.astro";
import { getCollection, render } from "astro:content";

export async function getStaticPaths() {
  const projects = await getCollection("projects");
  return projects.map((project) => ({
    params: { slug: project.slug },
    props: { project }
  }));
}

const { project } = Astro.props;
const { Content } = await render(project);
---

<BaseLayout title={`${project.data.title} | Infra Lab Portfolio`} description={project.data.summary}>
  <article class="mx-auto max-w-3xl">
    <header class="border-b border-line pb-10">
      <a class="text-sm font-semibold text-signal hover:text-green-700" href="/projects">
        <span data-lang-content="en">Back to projects</span>
        <span data-lang-content="zh">回到專案列表</span>
      </a>
      <p class="mt-8 font-mono text-sm uppercase tracking-wider text-neutral-500">
        <span data-lang-content="en">{project.data.status}</span>
        <span data-lang-content="zh">{project.data.statusZh ?? project.data.status}</span>
      </p>
      <h1 class="mt-4 text-4xl font-semibold leading-tight text-ink md:text-6xl">
        <span data-lang-content="en">{project.data.title}</span>
        <span data-lang-content="zh">{project.data.titleZh ?? project.data.title}</span>
      </h1>
      <p class="mt-6 text-xl leading-9 text-neutral-700">
        <span data-lang-content="en">{project.data.subtitle}</span>
        <span data-lang-content="zh">{project.data.subtitleZh ?? project.data.subtitle}</span>
      </p>
      <div class="mt-6 flex flex-wrap gap-2">
        {project.data.stack.map((item: string) => (
          <span class="rounded bg-neutral-100 px-2.5 py-1 text-xs text-neutral-700">{item}</span>
        ))}
      </div>
    </header>
    <div class="article-content mt-10">
      <Content />
    </div>
  </article>
</BaseLayout>
